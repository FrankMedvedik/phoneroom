<UserControl x:Class="PhoneLogic.Core.Views.BuildInformationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="clr-namespace:PhoneLogic.Core.Converters"
    mc:Ignorable="d">
    <Control.Resources>
        <converters:BooleanToVisibility x:Key="BoolToVisConverter" />
        <converters:NotBooleanToVisibility x:Key="NotBoolToVisConverter" />
    </Control.Resources>
    <Grid x:Name="LayoutRoot" Background="White" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Click="tbtnTest_Checked" HorizontalAlignment="Left" VerticalAlignment="Center" Width="Auto" Height="Auto" Margin="10"  FontSize="12" Content="?"/>
        <!--<ToggleButton x:Name="tbtnShowDebug"  HorizontalAlignment="Left"  VerticalAlignment="Center" 
                      Checked="tbtnTest_Checked" Unchecked="tbtnTest_Unchecked" Grid.Row="0" Content="(i)" Width="Auto"></ToggleButton>
            <StackPanel Orientation="Horizontal" Name="InfoStackPanel" Visibility="Collapsed" Grid.Row="1">
            <Border BorderThickness="1">
            <TextBlock Name="ReleaseInfo" />
            </Border>
        </StackPanel>-->
    </Grid>
</UserControl>
